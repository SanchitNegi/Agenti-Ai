Instructions
When this command is run with a feature name (e.g. /feature invoice):

Read CLAUDE.md in the project root to confirm structure rules
Use $ARGUMENTS as the feature name in lowercase (e.g. invoice)
Use $ARGUMENTS capitalised as the class/interface name (e.g. Invoice)
Create ALL four files below — never skip any
Update src/routes/index.ts to register the new route
Update src/types/index.ts to add the new interfaces
Print a summary of everything created